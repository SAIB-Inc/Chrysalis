[package]
name = "pallas-chainsync-bench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pallas = "1.1.0"
hex = "0.4.3"
tokio = { version = "1.27.0", features = ["rt-multi-thread", "macros", "signal"] }
